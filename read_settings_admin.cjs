const admin = require('firebase-admin');

// We can just initialize without credentials if we run this via something that has default creds? 
// No, but I can use firebase CLI to export the database!
